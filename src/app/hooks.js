import { useDispatch, useSelector } from "react-redux";

// Define the useAppDispatch function
export const useAppDispatch = useDispatch;

// Define the useAppSelector function
export const useAppSelector = useSelector;
